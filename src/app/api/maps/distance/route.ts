import { NextResponse } from 'next/server';

export async function POST() {
  // Phase 9: proxy to Google Distance Matrix API with server key
  return NextResponse.json({ error: 'Not implemented' }, { status: 501 });
}
